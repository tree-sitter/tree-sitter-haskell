=========================================
cpp: restore layout
=========================================

a = do
  do
    do
      a <- a
#ifndef Aaaa
    a <- a
#elif Aaa
    a <- a
    a
  a
#else
    a <- a
    a
  a
#endif
    a <- a
    a
  a

---

(module
 (decl_fun
  (funvar (variable))
  (funrhs
   (equals)
   (exp_do
    (stmt
     (exp_do
      (stmt
       (exp_do
        (stmt (bind_pattern (pat_name (variable)) (larrow) (exp_name (variable))))
        (cpp)))
      (stmt (bind_pattern (pat_name (variable)) (larrow) (exp_name (variable))))
      (cpp)
      (cpp)
      (stmt (bind_pattern (pat_name (variable)) (larrow) (exp_name (variable))))
      (stmt (exp_name (variable)))))
  (stmt (exp_name (variable)))))))

=========================================
cpp: multiline
=========================================

#if a \
a \
a

a = a

#endif

---

(module (cpp) (decl_fun (funvar (variable)) (funrhs (equals) (exp_name (variable)))) (cpp))
