=================================================
consym: valid
=================================================

data A = Int :+ Int

---

(module
 (adt
  (type)
  (equals)
  (constrs
   (constr_infix
    (type_name (type))
    (consym)
    (type_name (type))))))


=================================================
consym: error: ::
=================================================

data A = Int :: Int

---

(module (adt (type) (equals) (constrs (data_constructor (constructor) (ERROR (annotation)) (type_name (type))))))
