=========================================
pattern synonym: unidirectional simple
=========================================

pattern A a <- a:as

---

(module
 (pattern_synonym
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (larrow)
  (pat_infix (pat_name (varid)) (consym) (pat_name (varid)))))

=========================================
pattern synonym: unidirectional strict
=========================================

pattern A a = A !a

---

(module
 (pattern_synonym
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (pat_apply (pat_name (constructor)) (pat_strict (pat_name (varid))))))

=========================================
pattern synonym: explicit bidirectional list
=========================================

pattern HeadC a <- a:as where
  HeadC a = [a]

---

(module
 (pattern_synonym
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (larrow)
  (pat_infix (pat_name (varid)) (consym) (pat_name (varid)))
  (where)
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (funrhs (equals) (exp_list (exp_name (varid))))))

=========================================
pattern synonym: explicit bidirectional strict
=========================================

pattern A a <- A !a where
  A !a = A a

---

(module
 (pattern_synonym
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (larrow)
  (pat_apply (pat_name (constructor)) (pat_strict (pat_name (varid))))
  (where)
  (pat_apply (pat_name (constructor)) (pat_strict (pat_name (varid))))
  (funrhs (equals) (exp_apply (exp_name (constructor)) (exp_name (varid))))))

=========================================
pattern synonym: explicit bidirectional record
=========================================

pattern A { a } <- A a where
  A a = if a >= 0 then a else a

---

(module
 (pattern_synonym
  (pat_name (constructor))
  (pat_record (fpat (varid)))
  (larrow)
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (where)
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (funrhs
   (equals)
   (exp_cond
    if: (exp_infix (exp_name (varid)) (qvarop (varsym)) (exp_literal (integer)))
    then: (exp_name (varid))
    else: (exp_name (varid))))))

=========================================
pattern synonym: explicit bidirectional guards
=========================================

pattern A a <- A a where
  A a
    | a >= 0    = (A a)
    | otherwise = (A a)

---

(module
 (pattern_synonym
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (larrow)
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (where)
  (pat_apply (pat_name (constructor)) (pat_name (varid)))
  (funrhs
   (gdrhs
    (guards (guard (exp_infix (exp_name (varid)) (qvarop (varsym)) (exp_literal (integer)))))
    (equals)
    (exp_parens (exp_apply (exp_name (constructor)) (exp_name (varid)))))
   (gdrhs
    (guards (guard (exp_name (varid))))
    (equals)
    (exp_parens (exp_apply (exp_name (constructor)) (exp_name (varid))))))))

=========================================
pattern synonym: signature
=========================================

pattern A :: A -> A -> (A, A)

---

(module
 (pattern_synonym
  (constructor)
  (annotation)
  (fun
   (type_name (type))
   (fun
    (type_name (type))
    (tuple_type
     (type_name (type))
     (comma)
     (type_name (type)))))))

=========================================
pattern synonym: unidirectional record
=========================================

pattern A {a, a} = (a, a)

---

(module
 (pattern_synonym
  (pat_name (constructor)) (pat_record (fpat (varid)) (comma) (fpat (varid)))
  (pat_tuple (pat_name (varid)) (comma) (pat_name (varid)))))
